/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
module.exports = {
    images: {
      domains: ['localhost'], // Allow images from localhost
    },
  };
  